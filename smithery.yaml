# smithery.yaml
startCommand:
  type: stdio
  config:
    command: uvx
    args:
      - quickerspot-mcp
    env:
      QUICKERSPOT_API_KEY:
        type: string
        description: Sua API Key gerada no painel do QuickerSpot (https://quickerspot.com/settings)
        required: true
